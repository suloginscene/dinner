spring:
  profiles:
    active: test
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql://dinnerTest
dinner:
  url: http://localhost:8080
